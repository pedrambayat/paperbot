# Runtime dependencies for the Vercel serverless functions (api/).
# Socket Mode (slack-bolt) is intentionally excluded — the webhook worker
# talks to Slack via slack-sdk's WebClient.
certifi>=2024.2.2
curl-cffi>=0.7
httpx>=0.27
openai>=2.0
slack-sdk>=3.21
qstash>=2.0
